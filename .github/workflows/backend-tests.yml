name: Backend Tests

on:
  pull_request:
    paths:
      - "backend/**"
      - "test/**"
      - ".github/workflows/backend-tests.yml"
  push:
    branches:
      - main
      - prod
    paths:
      - "backend/**"
      - "test/**"
      - ".github/workflows/backend-tests.yml"

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install uv
          uv sync --locked

      - name: Tenant baseline table check
        run: python scripts/check_tenant_baseline.py

      - name: Run unit tests
        run: |
          cd ..
          PYTHONPATH=backend:. uv run --directory backend python -m unittest discover -s test/unit -p "test_*.py" -v
